# 📁 Структура проекта

```
lunch_bot/
│
├── main.py                 # Главный файл - точка входа в приложение
├── config.py               # Конфигурация (загрузка из .env)
├── database.py             # Работа с базой данных SQLite
├── handlers.py             # Обработчики команд для пользователей
├── admin_handlers.py       # Обработчики команд для администраторов
├── scheduler.py            # Планировщик уведомлений
│
├── requirements.txt        # Зависимости Python
├── .env.example           # Пример файла конфигурации
├── .env                   # Ваша конфигурация (создается вручную)
├── .gitignore            # Игнорируемые файлы для Git
│
├── start.sh              # Скрипт для быстрого запуска
├── README.md             # Полная документация
├── QUICKSTART.md         # Быстрый старт
└── STRUCTURE.md          # Этот файл

После запуска появится:
├── lunch_bot.db          # База данных SQLite
└── __pycache__/          # Кэш Python
```

## 🔍 Описание модулей

### main.py
- Инициализация бота
- Регистрация всех обработчиков команд
- Настройка ConversationHandler'ов
- Запуск планировщика уведомлений
- Точка входа в приложение

### config.py
- Загрузка переменных окружения из .env
- Хранение настроек:
  - BOT_TOKEN - токен Telegram бота
  - ADMIN_ID - ID администратора
  - LUNCH_TIME - время обеда
  - TIMEZONE - часовой пояс
  - DATABASE_NAME - имя файла базы данных

### database.py
Класс `Database` с методами для работы с данными:

**Пользователи:**
- `add_user()` - добавить пользователя
- `is_admin()` - проверка прав администратора
- `get_all_users()` - список всех пользователей

**Рестораны:**
- `add_restaurant()` - добавить ресторан
- `get_restaurant()` - получить ресторан
- `get_all_restaurants()` - список ресторанов
- `update_restaurant()` - обновить данные
- `delete_restaurant()` - удалить (деактивировать)

**Меню:**
- `add_menu_item()` - добавить блюдо
- `get_restaurant_menu()` - получить меню ресторана
- `update_menu_item()` - обновить блюдо
- `delete_menu_item()` - удалить блюдо

**Голосования:**
- `create_poll()` - создать голосование
- `get_active_poll()` - получить активное голосование
- `add_vote()` - добавить голос
- `get_poll_votes()` - результаты голосования
- `close_poll()` - закрыть голосование

**Участники:**
- `add_participant()` - записать на обед
- `remove_participant()` - отменить участие
- `get_participants()` - список участников
- `is_participant()` - проверка участия

### handlers.py
Обработчики команд для всех пользователей:

**Основные:**
- `/start` - регистрация и приветствие
- `/help` - справка по командам

**Голосование:**
- `/lunch` - начать/показать голосование
- `vote_callback` - обработка нажатия на кнопку ресторана
- `/results` - результаты голосования
- `show_results_callback` - результаты через callback

**Участие:**
- `/join` - записаться на обед
- `/participants` - список участников
- `show_participants_callback` - участники через callback
- `leave_callback` - отменить участие
- `/cancel` - отменить участие (команда)

**Меню:**
- `/menu` - выбор ресторана для просмотра меню
- `show_menu_callback` - показ меню ресторана

### admin_handlers.py
Обработчики команд для администраторов:

**Панель:**
- `/admin` - главная панель администратора
- Декоратор `@admin_only` - проверка прав

**Рестораны:**
- `/add_restaurant` - добавление ресторана (ConversationHandler)
  - `restaurant_name` - ввод названия
  - `restaurant_desc` - ввод описания
  - `restaurant_address` - ввод адреса
  - `restaurant_phone` - ввод телефона и сохранение
- `/list_restaurants` - список всех ресторанов

**Меню:**
- `/add_menu` - добавление блюда (ConversationHandler)
  - `menu_restaurant_selected` - выбор ресторана
  - `menu_item_name` - ввод названия блюда
  - `menu_item_price` - ввод цены
  - `menu_item_desc` - ввод описания
  - `menu_item_category` - ввод категории и сохранение

**Статистика:**
- `admin_stats_callback` - показ статистики
- `admin_users_callback` - список пользователей

**Отмена:**
- `cancel_admin` - отмена операции

### scheduler.py
Класс `LunchScheduler` для автоматических уведомлений:

**Методы:**
- `start()` - запуск планировщика
- `send_lunch_notification()` - уведомление в время обеда
- `send_voting_reminder()` - напоминание за 30 минут
- `stop()` - остановка планировщика

**Задачи:**
1. За 30 минут до обеда - напоминание о голосовании
2. В время обеда - объявление победителя и списка участников

## 💾 Структура базы данных

### Таблица: users
- `user_id` (INTEGER, PK) - Telegram ID
- `username` (TEXT) - username
- `first_name` (TEXT) - имя
- `last_name` (TEXT) - фамилия
- `is_admin` (INTEGER) - флаг администратора
- `created_at` (TIMESTAMP) - дата регистрации

### Таблица: restaurants
- `id` (INTEGER, PK) - ID ресторана
- `name` (TEXT) - название
- `description` (TEXT) - описание
- `address` (TEXT) - адрес
- `phone` (TEXT) - телефон
- `is_active` (INTEGER) - активен ли
- `created_at` (TIMESTAMP) - дата добавления

### Таблица: menu_items
- `id` (INTEGER, PK) - ID блюда
- `restaurant_id` (INTEGER, FK) - ID ресторана
- `name` (TEXT) - название блюда
- `description` (TEXT) - описание
- `price` (REAL) - цена
- `category` (TEXT) - категория
- `is_available` (INTEGER) - доступно ли
- `created_at` (TIMESTAMP) - дата добавления

### Таблица: polls
- `id` (INTEGER, PK) - ID голосования
- `created_by` (INTEGER, FK) - кто создал
- `date` (DATE) - дата голосования
- `status` (TEXT) - статус (active/closed)
- `winner_restaurant_id` (INTEGER, FK) - победитель
- `created_at` (TIMESTAMP) - дата создания
- `closed_at` (TIMESTAMP) - дата закрытия

### Таблица: votes
- `id` (INTEGER, PK) - ID голоса
- `poll_id` (INTEGER, FK) - ID голосования
- `user_id` (INTEGER, FK) - ID пользователя
- `restaurant_id` (INTEGER, FK) - ID ресторана
- `created_at` (TIMESTAMP) - дата голоса
- UNIQUE(poll_id, user_id) - один голос на голосование

### Таблица: lunch_participants
- `id` (INTEGER, PK) - ID записи
- `poll_id` (INTEGER, FK) - ID голосования
- `user_id` (INTEGER, FK) - ID пользователя
- `created_at` (TIMESTAMP) - дата записи
- UNIQUE(poll_id, user_id) - одна запись на голосование

## 🔄 Поток работы

### 1. Утро (10:00-11:30)
```
Пользователь -> /lunch
    ↓
Бот создает голосование
    ↓
Показывает список ресторанов (inline кнопки)
    ↓
Пользователь голосует
    ↓
БД: добавляется голос + автоматически добавляется в участники
```

### 2. Напоминание (11:30)
```
Scheduler -> send_voting_reminder()
    ↓
Получает всех пользователей из БД
    ↓
Отправляет напоминание каждому
```

### 3. Время обеда (12:00)
```
Scheduler -> send_lunch_notification()
    ↓
Получает активное голосование
    ↓
Подсчитывает голоса
    ↓
Определяет победителя
    ↓
Закрывает голосование
    ↓
Отправляет уведомления участникам
```

## 🛠️ Расширение функционала

### Добавление новой команды для пользователей

1. Создайте функцию в `handlers.py`:
```python
async def my_command(update: Update, context: ContextTypes.DEFAULT_TYPE):
    await update.message.reply_text("Привет!")
```

2. Зарегистрируйте в `main.py`:
```python
application.add_handler(CommandHandler("mycommand", my_command))
```

### Добавление нового поля в базу данных

1. Добавьте поле в `database.py` в `init_db()`:
```python
cursor.execute('''
    ALTER TABLE restaurants ADD COLUMN rating REAL DEFAULT 0
''')
```

2. Обновите методы работы с таблицей

### Добавление нового уведомления

1. Создайте метод в `scheduler.py`:
```python
async def send_custom_notification(self):
    # Ваша логика
    pass
```

2. Добавьте задачу в `start()`:
```python
self.scheduler.add_job(
    self.send_custom_notification,
    trigger=CronTrigger(hour=15, minute=0),
    id='custom_notification'
)
```

## 📦 Зависимости

- **python-telegram-bot** (20.7) - API для Telegram ботов
- **python-dotenv** (1.0.0) - Загрузка .env файлов
- **APScheduler** (3.10.4) - Планировщик задач

## 🔐 Безопасность

- Все чувствительные данные в `.env` (не коммитятся в Git)
- База данных локальная (SQLite)
- Проверка прав администратора через декоратор
- Валидация входных данных в обработчиках

---

**Понимание структуры поможет вам легко расширять функционал бота!** 🚀

