# 🧪 Тестирование бота

## 🚀 Запуск и тестирование

### 1️⃣ Получение токена от BotFather

1. Откройте Telegram
2. Найдите бота **@BotFather** (с синей галочкой)
3. Отправьте команду: `/newbot`
4. Придумайте имя: `My Lunch Bot` (любое)
5. Придумайте username: `my_company_lunch_bot` (должен заканчиваться на `bot`)
6. **Скопируйте токен!** Выглядит так:
   ```
   123456789:ABCdefGHIjklMNOpqrsTUVwxyz1234567
   ```

### 2️⃣ Получение вашего Telegram ID

1. Найдите бота **@userinfobot**
2. Отправьте ему любое сообщение
3. **Скопируйте ваш ID** (число, например: `123456789`)

### 3️⃣ Настройка .env файла

```bash
cd /Users/sevak.martirosyan/lunch_bot

# Создайте .env из примера
cp .env.example .env

# Откройте файл в редакторе
nano .env
# или
open -e .env  # откроется в TextEdit на Mac
```

Вставьте ваши данные:

```env
BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz1234567
ADMIN_ID=123456789
LUNCH_TIME=12:00
TIMEZONE=Europe/Moscow
```

Сохраните файл (в nano: Ctrl+O, Enter, Ctrl+X)

### 4️⃣ Запуск бота

**Вариант А: Автоматический запуск (рекомендуется)**

```bash
cd /Users/sevak.martirosyan/lunch_bot
./start.sh
```

**Вариант Б: Ручной запуск**

```bash
cd /Users/sevak.martirosyan/lunch_bot

# Создайте виртуальное окружение
python3 -m venv venv

# Активируйте его
source venv/bin/activate

# Установите зависимости
pip install -r requirements.txt

# Запустите бота
python main.py
```

### 5️⃣ Проверка запуска

Если всё правильно, вы увидите:

```
INFO - База данных инициализирована
INFO - Планировщик запущен. Уведомления в 12:00
INFO - Бот запущен и готов к работе!
```

---

## 🧪 Тестирование функционала

### Тест 1: Регистрация пользователя

1. Найдите вашего бота в Telegram по username
2. Отправьте: `/start`

**Ожидаемый результат:**
```
Привет, [Ваше Имя]! 👋

Я бот для организации совместных обедов в вашей компании.

📋 Доступные команды:
...
```

✅ **Успешно!** Вы зарегистрированы в системе.

---

### Тест 2: Добавление ресторана (администратор)

Отправьте: `/add_restaurant`

**Диалог:**

```
> Введите название ресторана:
Тестовая Пиццерия

> Введите описание:
Лучшая пицца в городе

> Введите адрес:
ул. Тестовая, д. 1

> Введите телефон:
+7 (999) 123-45-67
```

**Ожидаемый результат:**
```
✅ Ресторан Тестовая Пиццерия успешно добавлен!
```

---

### Тест 3: Добавление меню

Отправьте: `/add_menu`

**Диалог:**

```
> [Выберите "Тестовая Пиццерия" из кнопок]

> Введите название блюда:
Маргарита

> Введите цену:
450

> Введите описание:
Классическая пицца с моцареллой

> Введите категорию:
Пицца
```

**Ожидаемый результат:**
```
✅ Блюдо Маргарита (450 ₽) успешно добавлено!
```

**Повторите** для добавления еще 2-3 блюд.

---

### Тест 4: Добавление еще ресторанов

Добавьте еще 2-3 ресторана для тестирования голосования:

```bash
/add_restaurant
Суши Бар
Японская кухня
ул. Тестовая, д. 2
+7 (999) 234-56-78

/add_restaurant
Бургерная
Американская кухня
ул. Тестовая, д. 3
+7 (999) 345-67-89
```

---

### Тест 5: Просмотр меню

Отправьте: `/menu`

**Ожидаемый результат:**
- Появятся кнопки с ресторанами
- Нажмите на любой ресторан
- Увидите меню с ценами и категориями

---

### Тест 6: Голосование

Отправьте: `/lunch`

**Ожидаемый результат:**
```
🗳️ Голосование за ресторан

Выберите ресторан для сегодняшнего обеда:
```

- Появятся кнопки с ресторанами
- Нажмите на любой ресторан

**После голосования:**
```
✅ Вы проголосовали за Тестовая Пиццерия
Вы записаны на обед! 🍽️
```

---

### Тест 7: Результаты голосования

Отправьте: `/results`

**Ожидаемый результат:**
```
📊 Результаты голосования:

1. Тестовая Пиццерия
   🟩 1 голос(ов)

👥 Участников обеда: 1

🏆 Лидирует: Тестовая Пиццерия
```

---

### Тест 8: Список участников

Отправьте: `/participants`

**Ожидаемый результат:**
```
👥 Участники обеда:

1. [Ваше имя] (@username)

Всего: 1 человек
```

---

### Тест 9: Отмена участия

Отправьте: `/cancel`

**Ожидаемый результат:**
```
✅ Вы отменили участие в обеде.
```

Отправьте: `/join` (чтобы записаться снова)

---

### Тест 10: Панель администратора

Отправьте: `/admin`

**Ожидаемый результат:**
- Панель с кнопками:
  - 🏪 Рестораны
  - 📋 Меню
  - 📊 Статистика
  - 👥 Пользователи

Попробуйте нажать на каждую кнопку.

---

### Тест 11: Список ресторанов

Отправьте: `/list_restaurants`

**Ожидаемый результат:**
```
🏪 Список ресторанов:

✅ Тестовая Пиццерия (ID: 1)
   📝 Лучшая пицца в городе
   📍 ул. Тестовая, д. 1
   📞 +7 (999) 123-45-67

✅ Суши Бар (ID: 2)
   ...
```

---

## 🔔 Тестирование уведомлений

### Быстрый тест уведомлений

Чтобы протестировать уведомления **прямо сейчас**, измените время в `.env`:

1. Откройте `.env`:
```bash
nano .env
```

2. Измените `LUNCH_TIME` на время через 2 минуты от текущего:
```env
LUNCH_TIME=14:35  # если сейчас 14:33
```

3. Перезапустите бота:
```bash
# Остановите: Ctrl+C
# Запустите снова:
python main.py
```

4. Подождите 2 минуты и проверьте Telegram - должно прийти уведомление!

---

## 🧪 Тестирование с несколькими пользователями

### Способ 1: Пригласите коллег

1. Дайте username вашего бота коллегам
2. Попросите их отправить `/start`
3. Каждый голосует: `/lunch`
4. Проверьте результаты: `/results`

### Способ 2: Используйте второй аккаунт

1. Установите Telegram на телефон
2. Используйте другой номер телефона
3. Найдите бота и тестируйте

---

## ✅ Чек-лист тестирования

- [ ] Бот запускается без ошибок
- [ ] `/start` - регистрация работает
- [ ] `/add_restaurant` - можно добавить ресторан
- [ ] `/add_menu` - можно добавить блюда
- [ ] `/menu` - меню отображается корректно
- [ ] `/lunch` - голосование работает
- [ ] Inline кнопки нажимаются и работают
- [ ] `/results` - результаты отображаются
- [ ] `/participants` - список участников корректен
- [ ] `/join` и `/cancel` работают
- [ ] `/admin` - панель администратора доступна
- [ ] Уведомления приходят в заданное время

---

## 🐛 Решение проблем

### Бот не запускается

**Ошибка:** `BOT_TOKEN not found`

```bash
# Проверьте .env файл
cat .env

# Убедитесь что токен правильный
# Должно быть: BOT_TOKEN=123456789:ABC...
```

**Ошибка:** `ModuleNotFoundError: No module named 'telegram'`

```bash
# Установите зависимости
pip install -r requirements.txt
```

### Команды администратора не работают

```bash
# 1. Проверьте ADMIN_ID
cat .env | grep ADMIN_ID

# 2. Проверьте ваш настоящий ID через @userinfobot

# 3. Если не совпадает, исправьте .env и перезапустите бота
```

### Бот не отвечает

1. Проверьте что бот запущен (смотрите консоль)
2. Отправьте `/start` заново
3. Проверьте логи в консоли

### Уведомления не приходят

1. Убедитесь что формат времени правильный: `HH:MM`
2. Проверьте часовой пояс
3. Посмотрите логи при запуске:
   ```
   INFO - Планировщик запущен. Уведомления в 12:00
   ```

---

## 📊 Просмотр базы данных

```bash
cd /Users/sevak.martirosyan/lunch_bot

# Откройте базу данных
sqlite3 lunch_bot.db

# Посмотрите таблицы
.tables

# Посмотрите пользователей
SELECT * FROM users;

# Посмотрите рестораны
SELECT * FROM restaurants;

# Посмотрите голосования
SELECT * FROM polls;

# Выход
.quit
```

---

## 🎯 Тестовый сценарий "Реальный день"

### Утро (10:00)
1. Администратор проверяет рестораны: `/list_restaurants`
2. При необходимости добавляет новый ресторан

### Перед обедом (11:00)
1. Первый сотрудник запускает голосование: `/lunch`
2. Выбирает ресторан
3. Другие коллеги присоединяются

### Голосование (11:00-11:30)
1. Каждый голосует: `/lunch` → выбирает ресторан
2. Некоторые смотрят меню: `/menu`
3. Проверяют кто идёт: `/participants`

### Перед обедом (11:55)
1. Проверяют результаты: `/results`
2. Победитель определён!

### Обед (12:00)
1. Всем участникам приходит уведомление
2. Голосование автоматически закрывается

---

## 🔍 Отладка

### Включите подробное логирование

Измените в `main.py`:

```python
logging.basicConfig(
    format='%(asctime)s - %(name)s - %(levelname)s - %(message)s',
    level=logging.DEBUG  # было INFO
)
```

### Сохранение логов в файл

```bash
python main.py 2>&1 | tee bot.log
```

Теперь все логи сохраняются в `bot.log`

---

## 🎉 Готово!

Если все тесты прошли успешно - **бот полностью работает!** 🚀

**Что дальше?**
- Пригласите коллег
- Добавьте больше ресторанов
- Настройте время обеда под вашу компанию
- Наслаждайтесь удобными совместными обедами! 🍽️

---

## 📝 Чек-лист перед продакшеном

- [ ] Все команды протестированы
- [ ] Добавлено минимум 3 ресторана
- [ ] У каждого ресторана есть меню
- [ ] Уведомления приходят вовремя
- [ ] Настроен правильный часовой пояс
- [ ] Токен бота в безопасности (.env не коммитится)
- [ ] База данных работает корректно
- [ ] Бот запускается автоматически при старте сервера (опционально)

**Удачного тестирования!** 🧪✨

