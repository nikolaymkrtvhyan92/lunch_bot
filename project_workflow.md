# 🔄 Project Workflow — Рабочий процесс разработки

## 📖 Введение

Этот документ описывает **стандартизированный рабочий процесс** для проекта Lunch Bot. Следуя этим правилам, вы и AI-ассистент (Cursor) работаете как единая команда, где AI выступает в роли **AI-тимлида**, который знает архитектуру, соблюдает стандарты и ведёт документацию.

---

## 🎯 Философия работы

### Принципы:
1. **`cursor_context.md`** — единственный источник правды о проекте
2. **Каждое изменение** документируется автоматически
3. **Структурированная коммуникация** — всегда ясно и организованно
4. **Консистентность кода** — единый стиль во всём проекте
5. **Тестирование** — обязательная проверка перед коммитом

---

## 📁 Ключевые файлы проекта

| Файл | Назначение | Кто обновляет |
|------|-----------|---------------|
| `cursor_context.md` | Память проекта: контекст, архитектура, решения | AI автоматически |
| `project_workflow.md` | Рабочий процесс (этот файл) | Вручную при необходимости |
| `.cursor/rules/*.mdc` | Правила для AI-ассистента | Вручную при необходимости |
| `README.md` | Пользовательская документация | По мере релизов |
| `STRUCTURE.md` | Техническая архитектура | При изменении структуры |

---

## 🚀 Как ставить задачи AI (Cursor)

### ✅ Хорошие запросы

#### 1. Конкретная задача с контекстом
```
Добавь команду /stats для пользователей, которая показывает:
- Количество их голосований
- Самый популярный ресторан
- Дату первого участия в обеде

Используй существующую структуру handlers.py и стиль кода проекта.
```

#### 2. Исправление бага с деталями
```
В функции send_lunch_notification() не учитывается часовой пояс.
Пользователи в разных часовых поясах получают уведомления 
в неправильное время. Исправь это, используя TIMEZONE из config.py.
```

#### 3. Рефакторинг с обоснованием
```
Метод get_poll_votes() в database.py возвращает сырые данные.
Отрефактори его так, чтобы он возвращал словарь с подсчитанными
голосами: {restaurant_id: vote_count}. Обнови все места, где он используется.
```

#### 4. Новая фича с требованиями
```
Реализуй функцию "Избранные рестораны":
- Пользователь может добавить ресторан в избранное
- Команда /favorites показывает список избранных
- При голосовании избранные показываются первыми со звёздочкой ⭐
- Добавь таблицу favorites в БД
```

### ❌ Плохие запросы

```
❌ "Сделай лучше" — нет конкретики
❌ "Добавь что-нибудь интересное" — нет требований
❌ "Исправь" — не указано, что именно
❌ "Как в том проекте" — нет ссылки на референс
```

### 📝 Структура хорошего запроса

```
[Что нужно сделать]
Краткое описание задачи в 1-2 предложениях

[Детали]
- Конкретное требование 1
- Конкретное требование 2
- Конкретное требование 3

[Контекст]
Где в коде это находится, какие файлы затронуты

[Ожидаемый результат]
Как должно работать после изменений
```

---

## 🔄 Процесс разработки

### 1️⃣ Постановка задачи

```bash
# Вы описываете задачу AI:
"Добавь возможность администратору удалять рестораны через команду /delete_restaurant"
```

### 2️⃣ AI анализирует контекст

AI автоматически:
- ✅ Загружает `cursor_context.md`
- ✅ Изучает текущую архитектуру
- ✅ Проверяет существующий код в `admin_handlers.py`
- ✅ Смотрит на стиль кода в проекте

### 3️⃣ AI уточняет, если нужно

```
AI: "У нас два варианта удаления:
1. Мягкое удаление (is_active = 0) — ресторан скрыт, но данные сохранены
2. Жёсткое удаление (DELETE) — ресторан удаляется полностью

Какой вариант выбрать?"

Вы: "Мягкое удаление, чтобы сохранить историю"
```

### 4️⃣ AI реализует задачу

AI:
- 🔧 Создаёт код в соответствии со стилем проекта
- 📝 Добавляет комментарии на русском языке
- 🏗️ Следует архитектурным паттернам из `cursor_context.md`
- ✅ Использует декоратор `@admin_only` для защиты

### 5️⃣ AI обновляет документацию

AI автоматически обновляет `cursor_context.md`:
```markdown
## 📅 Лог изменений

### 2025-11-13
- ✅ Добавлена команда /delete_restaurant для администраторов
- Реализовано мягкое удаление (is_active = 0)
- Добавлен метод Database.delete_restaurant()
```

### 6️⃣ Тестирование

```bash
# Вы тестируете:
python main.py

# В Telegram:
/delete_restaurant
→ Выбираете ресторан
→ Проверяете, что он скрыт из списков
→ Проверяете, что старые голосования сохранены
```

### 7️⃣ Коммит (если всё работает)

```bash
git add .
git commit -m "feat: добавлена команда удаления ресторанов для админов"
git push
```

---

## 📋 Как обновляется cursor_context.md

### Автоматические обновления (AI делает сам)

AI обновляет файл в следующих случаях:

#### 1. После реализации задачи
```markdown
## 📅 Лог изменений
### 2025-11-13
- ✅ Добавлена команда /favorites
- Создана таблица favorites в БД
- Обновлён handlers.py (новая команда)
```

#### 2. При обнаружении бага
```markdown
## 🐛 Известные проблемы
- Уведомления не учитывают часовой пояс пользователя
  Статус: В работе
  Дата: 2025-11-13
```

#### 3. При изменении архитектуры
```markdown
## 🏗️ Архитектурные решения
### Система избранного
- Таблица favorites связывает user_id и restaurant_id
- При удалении ресторана избранное автоматически удаляется (CASCADE)
- Избранные показываются первыми в списке голосования
```

#### 4. При появлении новых идей
```markdown
## 💡 Идеи для улучшения
- [ ] Добавить рейтинг ресторанов от пользователей (1-5 звёзд)
- [ ] Интеграция с Яндекс.Картами для показа расстояния
- [ ] Групповой заказ с автоматическим расчётом на человека
```

#### 5. При принятии технических решений
```markdown
## ⚙️ Технические решения
### Работа с часовыми поясами
Решено: Использовать pytz и хранить TIMEZONE в .env
Причина: SQLite не поддерживает timezone-aware datetime
Дата: 2025-11-13
```

### Ручные обновления (вы делаете сами)

Вы обновляете файл, когда:
- 📝 Меняются бизнес-требования
- 🎯 Появляются новые стратегические цели
- 👥 Меняется команда или роли
- 🔐 Меняются правила безопасности

---

## 🎨 Стандарты написания кода

### Python Code Style

#### 1. Именование

```python
# ✅ Правильно (snake_case для функций и переменных)
def get_active_poll():
    user_name = "Ivan"
    restaurant_id = 123

# ❌ Неправильно
def getActivePoll():  # camelCase не используется в Python
    userName = "Ivan"  # camelCase не используется
```

#### 2. Структура функций

```python
# ✅ Правильно: docstring, типизация, async/await
async def add_restaurant(
    name: str,
    description: str = None,
    address: str = None,
    phone: str = None
) -> int:
    """
    Добавляет новый ресторан в базу данных.
    
    Args:
        name: Название ресторана
        description: Описание (опционально)
        address: Адрес (опционально)
        phone: Телефон (опционально)
    
    Returns:
        ID созданного ресторана
    """
    # Реализация
    pass

# ❌ Неправильно: нет docstring, нет типов
def add_restaurant(name, desc, addr, phone):
    pass
```

#### 3. Обработка ошибок

```python
# ✅ Правильно: try-except с логированием
import logging

async def send_notification(user_id: int, message: str):
    try:
        await bot.send_message(user_id, message)
        logging.info(f"Уведомление отправлено пользователю {user_id}")
    except Exception as e:
        logging.error(f"Ошибка отправки уведомления {user_id}: {e}")

# ❌ Неправильно: голый except, нет логов
async def send_notification(user_id, message):
    try:
        await bot.send_message(user_id, message)
    except:
        pass
```

#### 4. Работа с базой данных

```python
# ✅ Правильно: параметризованные запросы
def get_user(user_id: int) -> dict:
    cursor.execute(
        "SELECT * FROM users WHERE user_id = ?",
        (user_id,)
    )
    return cursor.fetchone()

# ❌ Неправильно: SQL-инъекция!
def get_user(user_id: int) -> dict:
    cursor.execute(f"SELECT * FROM users WHERE user_id = {user_id}")
    return cursor.fetchone()
```

#### 5. Асинхронный код

```python
# ✅ Правильно: все обработчики async
async def start_command(update: Update, context: ContextTypes.DEFAULT_TYPE):
    await update.message.reply_text("Привет!")

# ❌ Неправильно: синхронная функция для telegram-бота
def start_command(update: Update, context: ContextTypes.DEFAULT_TYPE):
    update.message.reply_text("Привет!")
```

### Структура модулей

#### Порядок импортов
```python
# 1. Стандартная библиотека
import logging
import os
from datetime import datetime

# 2. Сторонние библиотеки
from telegram import Update, InlineKeyboardButton
from telegram.ext import ContextTypes

# 3. Локальные модули
from config import BOT_TOKEN, ADMIN_ID
from database import Database
```

#### Комментарии

```python
# ✅ Хорошие комментарии (объясняют "почему")
# Проверяем активное голосование, чтобы избежать дубликатов
active_poll = db.get_active_poll()

# ❌ Плохие комментарии (объясняют "что" — и так видно из кода)
# Получаем активное голосование
active_poll = db.get_active_poll()
```

---

## 🧪 Процесс тестирования

### 1. Локальное тестирование

#### Перед запуском
```bash
# Проверка синтаксиса
python -m py_compile *.py

# Проверка зависимостей
pip install -r requirements.txt
```

#### Запуск бота
```bash
# В основном окне терминала
python main.py

# Вы должны увидеть:
# INFO - База данных инициализирована
# INFO - Планировщик запущен
# INFO - Бот запущен и готов к работе!
```

### 2. Ручное тестирование в Telegram

#### Тест для новой пользовательской команды
```
1. Откройте бота в Telegram
2. Отправьте команду (например, /favorites)
3. Проверьте ответ бота
4. Проверьте корректность данных
5. Попробуйте граничные случаи (пустой список, много элементов)
```

#### Тест для админской команды
```
1. Убедитесь, что ваш ADMIN_ID в .env правильный
2. Отправьте команду (например, /delete_restaurant)
3. Проверьте, что non-admin пользователи получают отказ
4. Проверьте корректность работы для админа
5. Проверьте изменения в БД
```

### 3. Проверка базы данных

```bash
# Открыть БД
sqlite3 lunch_bot.db

# Проверить структуру таблицы
.schema restaurants

# Проверить данные
SELECT * FROM restaurants WHERE is_active = 1;

# Выйти
.quit
```

### 4. Проверка логов

```bash
# Смотреть логи в реальном времени
tail -f bot.log

# Или при запуске
python main.py 2>&1 | tee bot.log
```

### 5. Чек-лист перед коммитом

- [ ] ✅ Код запускается без ошибок
- [ ] ✅ Новая функция работает корректно
- [ ] ✅ Старые функции не сломаны (регрессия)
- [ ] ✅ БД обновляется правильно
- [ ] ✅ Логи не содержат ошибок
- [ ] ✅ Код соответствует стилю проекта
- [ ] ✅ Добавлены комментарии где нужно
- [ ] ✅ `cursor_context.md` обновлён

---

## 🤝 Взаимодействие с AI

### Как AI помогает

#### 1. Анализ кода
```
Вы: "Почему функция send_lunch_notification() иногда не работает?"

AI: [анализирует код]
"Нашёл проблему в scheduler.py:
- Если нет активного голосования, функция выходит молча
- Нет логирования этого случая
- Рекомендую добавить logging.warning()

Исправить?"
```

#### 2. Рефакторинг
```
Вы: "В handlers.py много дублирующегося кода при работе с меню"

AI: [анализирует]
"Да, функции show_menu_callback() и menu_command() дублируют логику.
Могу вынести в отдельную функцию format_menu(restaurant_id).

План:
1. Создать format_menu() в handlers.py
2. Обновить обе функции
3. Добавить docstring

Выполнить?"
```

#### 3. Добавление фич
```
Вы: "Добавь систему рейтингов для ресторанов"

AI: "Для этого нужно:

1. БД (database.py):
   - Таблица ratings (id, user_id, restaurant_id, rating, created_at)
   - Метод add_rating()
   - Метод get_restaurant_rating()

2. Handlers (handlers.py):
   - Команда /rate_restaurant
   - Callback для выбора звёзд (1-5)

3. Админка (admin_handlers.py):
   - Показ среднего рейтинга в /list_restaurants

Начать с БД?"
```

#### 4. Отладка
```
Вы: "Бот не отвечает на команду /lunch"

AI: [проверяет код]
"Проблемы не нашёл в handlers.py.

Проверь:
1. Зарегистрирована ли команда в main.py?
   application.add_handler(CommandHandler('lunch', lunch_command))

2. Есть ли ошибки в логах?
   tail -f bot.log

3. Отправил ли ты /start для регистрации?

Показать логи?"
```

### Когда AI задаёт вопросы

AI может уточнить детали, если:
- 📝 Требования неоднозначны
- 🔀 Есть несколько вариантов реализации
- ⚠️ Изменения затрагивают архитектуру
- 🐛 Обнаружен потенциальный баг

**Это нормально!** AI работает как опытный разработчик, который уточняет детали перед кодингом.

### Как давать фидбек AI

#### ✅ Хороший фидбек
```
"Отлично, но:
1. Добавь проверку на пустой список избранных
2. Используй emoji ⭐ для избранных ресторанов
3. Ограничь максимум 10 избранных на пользователя"
```

#### ❌ Плохой фидбек
```
"Не то"  ← Что именно не то?
"Переделай"  ← Как именно?
"Плохо"  ← В чём проблема?
```

---

## 📊 Workflow для разных типов задач

### 🆕 Новая фича

```
1. [Вы] Описываете фичу с требованиями
2. [AI] Уточняет детали и предлагает план
3. [Вы] Утверждаете план или корректируете
4. [AI] Реализует (БД → бэкенд → handlers)
5. [AI] Обновляет cursor_context.md
6. [Вы] Тестируете
7. [Вы] Коммитите если OK
```

### 🐛 Исправление бага

```
1. [Вы] Описываете баг с шагами воспроизведения
2. [AI] Анализирует код и находит причину
3. [AI] Предлагает решение
4. [Вы] Подтверждаете
5. [AI] Исправляет и добавляет логирование
6. [Вы] Проверяете, что баг исправлен
7. [Вы] Коммитите
```

### ♻️ Рефакторинг

```
1. [Вы] Указываете, что нужно улучшить
2. [AI] Анализирует и предлагает варианты
3. [Вы] Выбираете подход
4. [AI] Рефакторит с сохранением функциональности
5. [Вы] Тестируете старые функции (нет регрессии)
6. [Вы] Коммитите
```

### 📚 Обновление документации

```
1. [Вы] Просите обновить документацию
2. [AI] Обновляет README.md / cursor_context.md / STRUCTURE.md
3. [Вы] Проверяете актуальность
4. [Вы] Коммитите
```

---

## 🔐 Безопасность и лучшие практики

### Что ВСЕГДА нужно делать

✅ **Использовать `.env` для секретов**
```python
# ✅ Правильно
from config import BOT_TOKEN

# ❌ Неправильно
BOT_TOKEN = "123456:ABC..."  # Хардкод токена!
```

✅ **Проверять права администратора**
```python
@admin_only
async def delete_restaurant(update, context):
    # Код доступен только админам
```

✅ **Параметризовать SQL-запросы**
```python
# ✅ Правильно
cursor.execute("SELECT * FROM users WHERE user_id = ?", (user_id,))

# ❌ SQL-инъекция!
cursor.execute(f"SELECT * FROM users WHERE user_id = {user_id}")
```

✅ **Логировать важные события**
```python
logging.info(f"Пользователь {user_id} проголосовал за {restaurant_id}")
logging.error(f"Ошибка отправки уведомления: {e}")
```

✅ **Обрабатывать ошибки**
```python
try:
    result = await some_operation()
except Exception as e:
    logging.error(f"Ошибка: {e}")
    await update.message.reply_text("Произошла ошибка. Попробуйте позже.")
```

### Что НИКОГДА нельзя делать

❌ **Коммитить `.env` файл**
```bash
# ✅ .env должен быть в .gitignore
echo ".env" >> .gitignore
```

❌ **Хранить пароли в коде**
❌ **Игнорировать ошибки (голый except)**
❌ **Использовать eval() или exec() с пользовательским вводом**
❌ **Давать всем права администратора**

---

## 🎓 Обучение и онбординг

### Для новых разработчиков

**Шаг 1: Изучите контекст**
1. Прочитайте `cursor_context.md` (вся суть проекта)
2. Прочитайте `project_workflow.md` (этот файл)
3. Прочитайте `STRUCTURE.md` (архитектура)

**Шаг 2: Настройте окружение**
1. Клонируйте репозиторий
2. Создайте `.env` из `.env.example`
3. Установите зависимости: `pip install -r requirements.txt`
4. Запустите бота: `python main.py`

**Шаг 3: Первая задача**
1. Возьмите простую задачу (например, добавить команду `/stats`)
2. Попросите AI помочь с реализацией
3. Следуйте workflow из этого документа
4. Создайте первый PR

### Для AI-ассистента (Cursor)

При первом запуске AI:
1. Загружает `cursor_context.md` — получает контекст проекта
2. Читает `.cursor/rules/*.mdc` — получает правила работы
3. Читает `project_workflow.md` (этот файл) — понимает процессы
4. Готов работать по стандартам проекта!

---

## 📈 Метрики качества

### Код считается хорошим, если:

- ✅ Запускается без ошибок
- ✅ Следует стилю проекта (snake_case, docstrings, async)
- ✅ Имеет комментарии где нужно
- ✅ Обрабатывает ошибки с логированием
- ✅ Покрыт ручным тестированием
- ✅ Не дублирует существующий функционал
- ✅ Документирован в `cursor_context.md`

### Проект считается здоровым, если:

- ✅ `cursor_context.md` актуален
- ✅ Нет критических багов в продакшене
- ✅ Все команды работают корректно
- ✅ База данных не содержит битых связей
- ✅ Логи не переполнены ошибками
- ✅ Документация соответствует коду

---

## 🚨 Troubleshooting

### Проблема: AI не следует стандартам проекта

**Решение:**
1. Проверьте, что `.cursor/rules/*.mdc` файлы на месте
2. Убедитесь, что `cursor_context.md` актуален
3. Явно укажите в запросе: "Используй стиль проекта из cursor_context.md"

### Проблема: AI предлагает решение, которое противоречит архитектуре

**Решение:**
1. Напомните AI: "Это противоречит архитектуре из cursor_context.md"
2. Попросите альтернативное решение
3. Если решение правильное — обновите `cursor_context.md`

### Проблема: Контекст проекта устарел

**Решение:**
1. Обновите `cursor_context.md` вручную или попросите AI
2. AI автоматически подхватит изменения
3. При необходимости обновите `STRUCTURE.md` и `README.md`

---

## 🎉 Заключение

Этот workflow делает разработку:

- 🚀 **Быстрой** — AI знает контекст и стандарты
- 🎯 **Точной** — всё документировано и стандартизировано
- 🔄 **Консистентной** — единый стиль во всём проекте
- 📚 **Понятной** — новые разработчики быстро вливаются
- 🤖 **Автоматизированной** — AI обновляет документацию сам

**Следуйте этому процессу**, и Cursor станет вашим AI-тимлидом, который:
- ✅ Знает проект лучше, чем новый разработчик
- ✅ Следует вашим стандартам
- ✅ Ведёт документацию автоматически
- ✅ Предлагает решения, соответствующие архитектуре

---

**Версия:** 1.0  
**Дата создания:** 13 ноября 2025  
**Последнее обновление:** 13 ноября 2025  

**Поддержка:** При вопросах обращайтесь к `cursor_context.md` или спрашивайте AI! 🤖

