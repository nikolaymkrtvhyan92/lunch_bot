# 🍽️ Телеграм Бот для Организации Обедов

Бот для голосования и организации совместных обедов в компании.

## 📋 Возможности

### Для всех пользователей:
- 🗳️ Голосование за ресторан
- 📋 Просмотр меню ресторанов с ценами
- ✅ Запись на обед
- 👥 Просмотр списка участников
- 📊 Результаты голосования в реальном времени
- 🔔 Автоматические уведомления о времени обеда

### Для администраторов:
- 🏪 Управление списком ресторанов
- 📝 Добавление и редактирование меню
- 👑 Панель администратора
- 📊 Статистика использования

## 🚀 Установка

### 1. Клонирование репозитория

```bash
git clone <your-repo-url>
cd lunch_bot
```

### 2. Установка зависимостей

```bash
pip install -r requirements.txt
```

Или создайте виртуальное окружение:

```bash
python -m venv venv
source venv/bin/activate  # На Windows: venv\Scripts\activate
pip install -r requirements.txt
```

### 3. Создание бота в Telegram

1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Следуйте инструкциям:
   - Придумайте имя для бота (например: "Lunch Voting Bot")
   - Придумайте username для бота (например: "company_lunch_bot")
4. Сохраните полученный токен

### 4. Получение вашего Telegram ID

1. Найдите бота [@userinfobot](https://t.me/userinfobot)
2. Отправьте ему любое сообщение
3. Скопируйте ваш ID

### 5. Настройка конфигурации

Скопируйте файл `.env.example` в `.env`:

```bash
cp .env.example .env
```

Отредактируйте файл `.env`:

```env
# Токен бота от BotFather
BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz

# Ваш Telegram ID (от @userinfobot)
ADMIN_ID=123456789

# Время уведомления о обеде (в формате HH:MM)
LUNCH_TIME=12:00

# Часовой пояс
TIMEZONE=Europe/Moscow
```

### 6. Запуск бота

```bash
python main.py
```

При успешном запуске вы увидите:

```
INFO - База данных инициализирована
INFO - Планировщик запущен. Уведомления в 12:00
INFO - Бот запущен и готов к работе!
```

## 📱 Использование

### Первый запуск

1. Найдите вашего бота в Telegram по username
2. Отправьте команду `/start`
3. Бот зарегистрирует вас в системе

### Команды для пользователей

| Команда | Описание |
|---------|----------|
| `/start` | Регистрация и приветствие |
| `/help` | Справка по командам |
| `/lunch` | Начать голосование за ресторан |
| `/menu` | Посмотреть меню ресторана |
| `/join` | Записаться на обед |
| `/participants` | Список участников обеда |
| `/results` | Результаты голосования |
| `/cancel` | Отменить участие в обеде |

### Команды для администраторов

| Команда | Описание |
|---------|----------|
| `/admin` | Панель администратора |
| `/add_restaurant` | Добавить новый ресторан |
| `/list_restaurants` | Список всех ресторанов |
| `/add_menu` | Добавить блюдо в меню |

## 🔧 Настройка для администратора

### Добавление ресторана

1. Отправьте команду `/add_restaurant`
2. Следуйте инструкциям бота:
   - Введите название ресторана
   - Введите описание (или `/skip`)
   - Введите адрес (или `/skip`)
   - Введите телефон (или `/skip`)

Пример:
```
/add_restaurant

> Введите название ресторана:
Итальянская кухня

> Введите описание:
Аутентичная итальянская кухня с пастой и пиццей

> Введите адрес:
ул. Тверская, д. 15

> Введите телефон:
+7 (495) 123-45-67
```

### Добавление меню

1. Отправьте команду `/add_menu`
2. Выберите ресторан из списка
3. Следуйте инструкциям:
   - Введите название блюда
   - Введите цену
   - Введите описание (или `/skip`)
   - Введите категорию (или `/skip`)

Пример:
```
/add_menu

> [Выбираете ресторан из кнопок]

> Введите название блюда:
Паста Карбонара

> Введите цену:
450

> Введите описание:
Классическая карбонара с беконом и пармезаном

> Введите категорию:
Основные блюда
```

## 🔔 Автоматические уведомления

Бот автоматически отправляет два типа уведомлений:

1. **За 30 минут до обеда** - напоминание о необходимости проголосовать
2. **В время обеда** - объявление победителя голосования и список участников

Время уведомлений настраивается в файле `.env` через параметр `LUNCH_TIME`.

## 💾 База данных

Бот использует SQLite базу данных (`lunch_bot.db`), которая создается автоматически при первом запуске.

База данных содержит:
- 👥 Пользователи
- 🏪 Рестораны
- 📋 Меню с ценами
- 🗳️ Голосования
- ✅ Участники обедов

## 🐛 Решение проблем

### Бот не запускается

**Проблема:** `BOT_TOKEN not found`
- **Решение:** Проверьте, что файл `.env` существует и содержит правильный токен

**Проблема:** `ModuleNotFoundError`
- **Решение:** Установите зависимости: `pip install -r requirements.txt`

### Бот не отвечает на команды

- Проверьте, что бот запущен (`python main.py`)
- Убедитесь, что вы отправили `/start` для регистрации
- Проверьте логи в консоли

### Не приходят уведомления

- Проверьте параметр `LUNCH_TIME` в `.env`
- Убедитесь, что часовой пояс `TIMEZONE` указан правильно
- Проверьте, что планировщик запустился (смотрите логи)

### Команды администратора не работают

- Убедитесь, что ваш `ADMIN_ID` указан правильно в `.env`
- Перезапустите бота после изменения `.env`
- Отправьте `/start` заново

## 📊 Пример рабочего процесса

1. **10:30** - Администратор или любой пользователь запускает `/lunch`
2. **10:30-11:30** - Коллеги голосуют за рестораны и записываются на обед
3. **11:30** - Бот отправляет напоминание всем
4. **12:00** - Бот подводит итоги, закрывает голосование и объявляет победителя
5. **12:00** - Участники получают уведомление с результатами и ссылкой на меню

## 🔒 Безопасность

- Токен бота хранится в `.env` файле (добавлен в `.gitignore`)
- База данных хранится локально
- Только администраторы могут управлять ресторанами и меню

## 🚀 Деплой на сервер

### Запуск в фоновом режиме

Используйте `screen` или `tmux`:

```bash
screen -S lunch_bot
python main.py
# Нажмите Ctrl+A, затем D для отсоединения
```

### Использование systemd (Linux)

Создайте файл `/etc/systemd/system/lunch_bot.service`:

```ini
[Unit]
Description=Lunch Telegram Bot
After=network.target

[Service]
Type=simple
User=yourusername
WorkingDirectory=/path/to/lunch_bot
ExecStart=/usr/bin/python3 /path/to/lunch_bot/main.py
Restart=always

[Install]
WantedBy=multi-user.target
```

Запустите сервис:

```bash
sudo systemctl enable lunch_bot
sudo systemctl start lunch_bot
sudo systemctl status lunch_bot
```

## 📝 Лицензия

Этот проект создан для внутреннего использования в компании.

## 🤝 Поддержка

Если у вас возникли вопросы или проблемы:
1. Проверьте раздел "Решение проблем"
2. Посмотрите логи в консоли
3. Обратитесь к администратору бота

## 📈 Будущие улучшения

- [ ] История голосований и статистика популярности ресторанов
- [ ] Возможность заказа еды прямо через бота
- [ ] Интеграция с картами для показа местоположения ресторанов
- [ ] Фотографии блюд в меню
- [ ] Бюджет обеда и расчет стоимости
- [ ] Аллергены и диетические предпочтения
- [ ] Множественные группы обедов
- [ ] Экспорт статистики в Excel

---

**Приятного аппетита!** 🍽️

