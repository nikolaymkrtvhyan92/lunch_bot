# 🚀 Быстрый старт

## За 5 минут до запуска

### 1. Получите токен бота

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте `/newbot`
3. Придумайте имя: `Company Lunch Bot`
4. Придумайте username: `your_company_lunch_bot`
5. **Скопируйте токен** (выглядит так: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

### 2. Узнайте свой Telegram ID

1. Откройте [@userinfobot](https://t.me/userinfobot) в Telegram
2. Отправьте любое сообщение
3. **Скопируйте ваш ID** (число, например: `123456789`)

### 3. Настройте бота

```bash
# Скопируйте пример конфигурации
cp .env.example .env

# Откройте .env в любом редакторе
nano .env  # или vim, или блокнот
```

Вставьте ваши данные:

```env
BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz  # Вставьте токен от BotFather
ADMIN_ID=123456789                               # Вставьте ваш ID
LUNCH_TIME=12:00                                 # Время обеда
TIMEZONE=Europe/Moscow                           # Ваш часовой пояс
```

### 4. Запустите бота

**Вариант 1: Автоматический запуск (рекомендуется)**
```bash
./start.sh
```

**Вариант 2: Ручной запуск**
```bash
# Создайте виртуальное окружение
python3 -m venv venv
source venv/bin/activate  # На Windows: venv\Scripts\activate

# Установите зависимости
pip install -r requirements.txt

# Запустите бота
python main.py
```

### 5. Проверьте работу

1. Найдите вашего бота в Telegram
2. Отправьте `/start`
3. Вы должны увидеть приветствие! 🎉

## 📋 Первая настройка (для администратора)

### Добавьте рестораны

```
/add_restaurant

Название: Пицца Хат
Описание: Пицца и паста
Адрес: ул. Ленина, 1
Телефон: +7 (495) 123-45-67
```

Повторите для других ресторанов.

### Добавьте меню

```
/add_menu

[Выберите ресторан]

Название блюда: Маргарита
Цена: 450
Описание: Классическая пицца с моцареллой
Категория: Пицца
```

Добавьте несколько блюд для каждого ресторана.

## ✅ Готово!

Теперь ваши коллеги могут:
- 🗳️ Голосовать за рестораны: `/lunch`
- 📋 Смотреть меню: `/menu`
- ✅ Записываться на обед: `/join`

## 🔧 Устранение проблем

### Бот не запускается

```bash
# Проверьте .env файл
cat .env

# Проверьте установлены ли зависимости
pip list | grep telegram

# Переустановите зависимости
pip install -r requirements.txt --force-reinstall
```

### Команды администратора не работают

- Убедитесь, что `ADMIN_ID` в `.env` совпадает с вашим ID
- Перезапустите бота после изменения `.env`
- Отправьте `/start` заново

### Не приходят уведомления

- Проверьте `LUNCH_TIME` в `.env`
- Убедитесь, что формат времени правильный: `12:00` (HH:MM)
- Проверьте часовой пояс `TIMEZONE`

## 📚 Подробная документация

Смотрите [README.md](README.md) для полной документации.

---

**Нужна помощь?** Проверьте логи в консоли, где запущен бот! 🔍

